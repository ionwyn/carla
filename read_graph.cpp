#include <iostream>
#include <fstream>
#include <sstream>
#include <utility>

using namespace std;

/* The undirected weighted graph G is a set of vertices with the properties:
	- Each vertex has a set of neighbours
	- A path between two adjacent vertices has a traversal cost
	- Each vertex is defined by coordinate (latitude, longitude)

*/

